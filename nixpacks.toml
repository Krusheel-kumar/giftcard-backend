providers = ["jdk17", "gradle"]

[phases.build]
cmds = [
    "chmod +x gradlew",
    "./gradlew clean build -x check -x test --no-daemon --info"
]

[start]
cmd = "java -Xmx256m -Xms128m -XX:TieredStopAtLevel=1 -Xss512k -jar build/libs/*.jar"
