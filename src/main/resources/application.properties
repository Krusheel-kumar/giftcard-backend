spring.application.name=giftcard-backend
server.port=${PORT:8081}

# PostgreSQL
spring.datasource.url=${DB_URL:jdbc:postgresql://ep-super-lake-b40lv2hz-pooler.c-6.us-east-2.aws.neon.tech:5432/neondb?sslmode=require}
spring.datasource.username=${DB_USERNAME:neondb_owner}
spring.datasource.password=${DB_PASSWORD:npg_QRhy4IT5ZfJr}

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Security Configuration
admin.password=${ADMIN_PASSWORD:1234}
jwt.secret=${JWT_SECRET:your_super_secret_key_that_needs_to_be_long_enough_for_hs512_alg}
msg91.auth-key=${MSG91_AUTH_KEY:557539A3jnNLJWr6a73367fP1}
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:5174,http://localhost:3000,https://giftcard.popobob.com,https://*.vercel.app}

server.forward-headers-strategy=framework

# Actuator (for Railway health checks)
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=never
