[build]
builder = "nixpacks"

[deploy]
# Apply memory limits only during the run phase to avoid crashing the Gradle build process
startCommand = "export JAVA_TOOL_OPTIONS=\"-Xmx256m -Xms128m -XX:TieredStopAtLevel=1 -Xss512k\" && java -jar build/libs/*.jar"
