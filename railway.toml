[build]
builder = "nixpacks"

# Tell nixpacks to install Java 17 and gradle
[build.nixpacks]
jdkVersion = "17"

[deploy]
# Run the jar directly with strict memory limits to reduce Railway costs
startCommand = "java -Xmx256m -Xms128m -XX:TieredStopAtLevel=1 -Xss512k -jar build/libs/*.jar"
